# Server configuration
PORT=3000

# CORS allowed origin (use * for development, set specific domain in production)
CORS_ORIGIN=*

# Meta Conversions API version (update as new versions are released)
META_CAPI_VERSION=v19.0
